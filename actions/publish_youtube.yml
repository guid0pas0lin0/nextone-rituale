name: Publish YouTube Short
on:
  schedule: [{ cron: "0 12 * * *" }]
jobs:
  yt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          python -m pip install google-api-python-client google-auth-oauthlib
          python scripts/upload_youtube.py
